pre-commit:
  commands:
    biome-check:
      glob: "src/**/*.{ts,tsx}"
      run: npx @biomejs/biome check --staged --write --no-errors-on-unmatched --colors=off
      stage_fixed: true
    check-types:
      run: npx tsc --noEmit --jsx react
